multiPrint
-----------------------
In projects with many map composer views could be annoying
to manually open and export them one at once.
The plugin allows to export multiple map composer views 
in pdf or img simply by selecting them from a list.